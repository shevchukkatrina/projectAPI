const eventsService = require('./events');
const usersService = require('./users');
const authService = require('./auth');

module.exports = {
    eventsService,
    usersService,
    authService,
};
